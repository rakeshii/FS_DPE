[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libreoffice"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:app --bind 0.0.0.0:${PORT} --timeout 120 --workers 2"
